server:
  port: 9000
zuul:
  routes:
    myProduct:
      path: /myProduct/**
      serviceId: product
      sensitiveHeaders:
zuul:
  sensitive-headers: 
#  ignored-patterns:
#    - /**/product/listForOrder
